apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
type: Opaque
stringData:
  # Placeholders. Learner Lab credentials expire every session; refresh with
  #   ./scripts/set-aws-credentials.ps1
  # which reads the [default] profile from ~/.aws/credentials and restarts the APIs.
  AWS_ACCESS_KEY_ID: "replace-me"
  AWS_SECRET_ACCESS_KEY: "replace-me"
  AWS_SESSION_TOKEN: "replace-me"
